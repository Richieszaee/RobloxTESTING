-- STEAL LITE OLD GUI + BEST RARITY
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local UIS = game:GetService("UserInputService")
local LP = Players.LocalPlayer

local AutoSteal = false
local BasePoint = nil
local TweenSpeed = 500
local TargetPlayer = nil

-- URUTAN RARITY
local RARITY_ORDER = {
    ["Eternal"] = 10, ["Divine"] = 9, ["Secret"] = 8, ["Cosmic"] = 7,
    ["Mythic"] = 6, ["Legendary"] = 5, ["Epic"] = 4, ["Rare"] = 3,
    ["Uncommon"] = 2, ["Common"] = 1
}

local InteractRemote = ReplicatedStorage:FindFirstChild("StealEgg") or ReplicatedStorage:FindFirstChild("Interact")

-- GUI LAMA
local gui = Instance.new("ScreenGui", LP.PlayerGui)
gui.Name = "StealLiteHub"
gui.ResetOnSpawn = false

local Main = Instance.new("Frame", gui)
Main.Size = UDim2.new(0, 260, 0, 280) -- dibesarin dikit
Main.Position = UDim2.new(0, 20, 0, 20)
Main.BackgroundColor3 = Color3.fromRGB(12, 12, 16)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true
Instance.new("UICorner", Main).CornerRadius = UDim.new(0,12)
local gradient = Instance.new("UIGradient", Main)
gradient.Color = ColorSequence.new{ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 180, 0)),ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 180, 255))}
gradient.Rotation = 45
Instance.new("UIStroke", Main).Color = Color3.fromRGB(255,255,255).Transparency = 0.3

Instance.new("TextLabel", Main).Size = UDim2.new(1,-40,0,35).Position = UDim2.new(0,10,0,5)
.Parent.Text = "STEAL LITE".Parent.TextColor3 = Color3.new(1,1,1)
.Parent.Font = Enum.Font.GothamBlack.Parent.TextSize = 16
.Parent.BackgroundTransparency = 1.Parent.TextXAlignment = Enum.TextXAlignment.Left

-- SET BASE
local setBtn = Instance.new("TextButton", Main)
setBtn.Size = UDim2.new(1,-20,0,40) setBtn.Position = UDim2.new(0,10,0,45)
setBtn.Text = "SET BASE POINT" setBtn.BackgroundColor3 = Color3.fromRGB(25,25,35)
setBtn.TextColor3 = Color3.new(1,1,1) setBtn.Font = Enum.Font.GothamBold setBtn.TextSize = 14
Instance.new("UICorner", setBtn).CornerRadius = UDim.new(0,8)
setBtn.MouseButton1Click:Connect(function()
	local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
	if hrp then BasePoint = hrp.CFrame + Vector3.new(0,3,0) setBtn.Text = "✓ SAVED" task.wait(0.8) setBtn.Text = "SET BASE POINT" end
end)

-- AUTO STEAL
local autoBtn = Instance.new("TextButton", Main)
autoBtn.Size = UDim2.new(1,-20,0,40) autoBtn.Position = UDim2.new(0,10,0,88)
autoBtn.Text = "AUTO STEAL: OFF" autoBtn.BackgroundColor3 = Color3.fromRGB(200,40,40)
autoBtn.TextColor3 = Color3.new(1,1,1) autoBtn.Font = Enum.Font.GothamBlack autoBtn.TextSize = 14
Instance.new("UICorner", autoBtn).CornerRadius = UDim.new(0,8)
autoBtn.MouseButton1Click:Connect(function()
	AutoSteal = not AutoSteal
	autoBtn.Text = "AUTO STEAL: "..(AutoSteal and "ON" or "OFF")
	TweenService:Create(autoBtn, TweenInfo.new(0.15), {BackgroundColor3 = AutoSteal and Color3.fromRGB(0,200,80) or Color3.fromRGB(200,40,40)}):Play()
end)

-- TWEEN TO BASE
local baseBtn = Instance.new("TextButton", Main)
baseBtn.Size = UDim2.new(1,-20,0,35) baseBtn.Position = UDim2.new(0,10,0,131)
baseBtn.Text = "TWEEN TO BASE" baseBtn.BackgroundColor3 = Color3.fromRGB(0,170,255)
baseBtn.TextColor3 = Color3.new(1,1,1) baseBtn.Font = Enum.Font.GothamBold baseBtn.TextSize = 13
Instance.new("UICorner", baseBtn).CornerRadius = UDim.new(0,8)
baseBtn.MouseButton1Click:Connect(function() if BasePoint then TweenTo(BasePoint) end)

-- TWEEN TO PLAYER
local playerBtn = Instance.new("TextButton", Main)
playerBtn.Size = UDim2.new(1,-20,0,35) playerBtn.Position = UDim2.new(0,10,0,169)
playerBtn.Text = "TWEEN TO PLAYER" playerBtn.BackgroundColor3 = Color3.fromRGB(255,140,0)
playerBtn.TextColor3 = Color3.new(1,1,1) playerBtn.Font = Enum.Font.GothamBold playerBtn.TextSize = 13
Instance.new("UICorner", playerBtn).CornerRadius = UDim.new(0,8)
playerBtn.MouseButton1Click:Connect(function()
	if TargetPlayer and TargetPlayer.Character then
		local hrp = TargetPlayer.Character:FindFirstChild("HumanoidRootPart")
		if hrp then TweenTo(hrp) end
	end
end)

-- GANTI TARGET PLAYER
local switchBtn = Instance.new("TextButton", Main)
switchBtn.Size = UDim2.new(1,-20,0,25) switchBtn.Position = UDim2.new(0,10,0,207)
switchBtn.Text = "Switch Target" switchBtn.BackgroundColor3 = Color3.fromRGB(50,50,60)
switchBtn.TextColor3 = Color3.new(1,1,1) switchBtn.Font = Enum.Font.Gotham switchBtn.TextSize = 11
Instance.new("UICorner", switchBtn).CornerRadius = UDim.new(0,6)
local playerList = Players:GetPlayers()
local pIndex = 1
switchBtn.MouseButton1Click:Connect(function()
	pIndex = pIndex % #playerList + 1
	TargetPlayer = playerList[pIndex]
	switchBtn.Text = "Target: "..TargetPlayer.Name
end)
if #playerList > 0 then TargetPlayer = playerList[1] switchBtn.Text = "Target: "..TargetPlayer.Name end

-- CLOSE
local closeBtn = Instance.new("TextButton", Main)
closeBtn.Size = UDim2.new(0,22,0,22) closeBtn.Position = UDim2.new(1,-27,0,7)
closeBtn.Text = "X" closeBtn.BackgroundColor3 = Color3.fromRGB(255,50,50)
closeBtn.TextColor3 = Color3.new(1,1,1) closeBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0,5)
closeBtn.MouseButton1Click:Connect(function() gui:Destroy() end)

-- SPEED INFO
local speedLabel = Instance.new("TextLabel", Main)
speedLabel.Size = UDim2.new(1,-20,0,18) speedLabel.Position = UDim2.new(0,10,0,235)
speedLabel.Text = "Speed: "..TweenSpeed.." | Scroll Mouse" speedLabel.TextColor3 = Color3.fromRGB(200,200,200)
speedLabel.Font = Enum.Font.Gotham speedLabel.TextSize = 11 speedLabel.BackgroundTransparency = 1

-- FUNCTIONS
local function TweenTo(pos)
	local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
	if not hrp then return 0 end
	local dist = (hrp.Position - pos.Position).Magnitude
	local time = math.clamp(dist / TweenSpeed, 0.05, 2)
	TweenService:Create(hrp, TweenInfo.new(time, Enum.EasingStyle.Linear), {CFrame = pos}):Play()
	return time
end

local function GetRarity(obj) return RARITY_ORDER[obj:GetAttribute("Rarity") or "Common"] or 1 end

local function FindBestEgg()
	local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
	if not hrp then return nil end
	local best, score = nil, 0
	for _,v in pairs(Workspace:GetDescendants()) do
		if v:IsA("Model") and v.Name:lower():find("egg") then
			local part = v:FindFirstChildWhichIsA("BasePart", true)
			if part then local s = GetRarity(v) if s > score then best = part score = s end end
		end
	end
	return best
end

-- LOOP
task.spawn(function()
	while true do task.wait(0.1)
		if AutoSteal then
			local egg = FindBestEgg()
			if egg then
				local t1 = TweenTo(egg) task.wait(t1 + 0.1)
				pcall(function() if InteractRemote then InteractRemote:FireServer(egg) end end)
				task.wait(0.2)
				if BasePoint then local t2 = TweenTo(BasePoint) task.wait(t2 + 0.2) end
			end
		end
	end
end)

-- GANTI SPEED
UIS.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseWheel then
		TweenSpeed = math.clamp(TweenSpeed + input.Position.Z * 50, 200, 900)
		speedLabel.Text = "Speed: "..TweenSpeed.." | Scroll Mouse"
	end
end)

print("[Steal Lite Old GUI] Loaded")
